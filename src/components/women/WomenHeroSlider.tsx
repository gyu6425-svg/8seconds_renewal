import { womenHeroSliderCards } from '../../data/women';
import SectionHeroSlider from '../product/SectionHeroSlider';

export default function WomenHeroSlider() {
    return <SectionHeroSlider cards={womenHeroSliderCards} />;
}
